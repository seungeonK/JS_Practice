// ternary operator '?'
// usage -> let result = condition ? value1 : value2;

let tobaccoAge = (age >= 22) ? true : false;

if (age >= 22) {
    tobaccoAge = true;
} else {
    tobaccoAge = false;
}

//they both same
